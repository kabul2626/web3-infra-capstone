apiVersion: v1
kind: Service
metadata:
  name: oracle-agent
  namespace: web3-infra
spec:
  selector:
    app: oracle-agent
  ports:
    - name: http
      port: 8081
      targetPort: 8081
  type: ClusterIP
