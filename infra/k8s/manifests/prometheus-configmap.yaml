apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config
  namespace: web3-infra
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s

    scrape_configs:
      - job_name: "oracle-agent"
        static_configs:
          - targets: ["oracle-agent:8081"]
