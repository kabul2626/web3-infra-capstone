apiVersion: v1
kind: Service
metadata:
  name: oracle-monitor
  namespace: web3-infra
spec:
  selector:
    app: oracle-monitor
  ports:
    - name: http
      port: 8082
      targetPort: 8082
  type: ClusterIP
