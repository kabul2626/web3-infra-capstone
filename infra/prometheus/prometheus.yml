global:
  scrape_interval: 15s

rule_files:
  - /etc/prometheus/alert.rules.yml

scrape_configs:
  - job_name: "oracle-agent"
    static_configs:
      - targets: ["agent:8081"]
  - job_name: "oracle-monitor"
    static_configs:
      - targets: ["monitor:8082"]
